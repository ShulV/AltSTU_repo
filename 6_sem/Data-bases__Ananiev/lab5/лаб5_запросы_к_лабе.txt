1)
INSERT INTO public."Direction"(
	"Name")
	VALUES ('ПИ-новое направление');
2)
UPDATE public."Direction"
	SET "Name"='ПИ-новое направление с новым наименованием'
	WHERE "Name" LIKE 'ПИ-н%'
3)
DELETE FROM public."Direction"
	WHERE "Name" = 'ФЭАТ';

ERROR: ОШИБКА:  UPDATE или DELETE в таблице "Direction" нарушает ограничение внешнего ключа "HasPart4" таблицы "Admiscontr"
DETAIL:  На ключ (PK_dir)=(10) всё ещё есть ссылки в таблице "Admiscontr".

ошибка ссылочной целостности
4)
SELECT dir."Name" as "Направление", count(st.*) as "Количество студентов"
	FROM public."Direction" as dir
		left join "Gr" as gr
			on dir."PK_dir" = gr."PK_dir"
				left join "Stud" as st
					on gr."PK_gr" = st."PK_gr"
	group by dir."Name"
	order by "Количество студентов"